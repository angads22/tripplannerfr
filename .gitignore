# dependencies
node_modules/

# runtime data: accounts, trips, sessions, the session secret — never commit these
data/

# local env overrides (secrets / invite code)
.env

# built exe output — the exe now ships via GitHub Releases, not the repo
dist/TripPlanner.exe
dist/TripPlanner-new.exe
dist/apply-update.bat
TripPlanner.exe
TripPlanner-new.exe
apply-update.bat

# the Cloudflare helper downloaded by the "Go Online" button
cloudflared.exe
cloudflared
# generated tunnel config + credentials (machine-specific, keep private)
cloudflared-config.yml
*.cloudflared.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
